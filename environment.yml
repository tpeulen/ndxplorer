name: ndxplorer
channels:
  - conda-forge
dependencies:
  - pip
  - qt >=5
  - qtpy
  - pyqt
  - numpy
  - pyyaml
  - pandas
  - pyqtgraph
  - pathlib2
  - typing-extensions
  - scipy
  - hdbscan
  - scikit-learn
  # Mask image I/O: TIFF via tttrlib's bundled libtiff.
  - tttrlib >=0.27

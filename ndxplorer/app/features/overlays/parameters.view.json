{
 "_comment": "The Parameters tab: nDXplorer's constants (Qt: ui/parameter_editor.py), in the app's shared parameter table (views/parameter_table.view.json). A vector constant is an expandable row with one child per population. An edit re-derives the columns that read the constant.",
 "sections": [
  {
   "type": "button_row",
   "buttons": [
    {
     "label": "Save",
     "action": "save_parameters",
     "description": "Save parameters"
    }
   ],
   "width": 130
  },
  {
   "type": "parameter_table",
   "table": "table",
   "expand": true,
   "reserve": 64
  },
  {
   "type": "info",
   "text": "Right-click a row to copy, paste, link, or make it a vector (one value per population)."
  },
  {
   "type": "row",
   "sections": [
    {
     "type": "button_row",
     "buttons": [
      {
       "label": "Add parameter",
       "action": "add_parameter",
       "description": "Add a new constant (name + value)"
      }
     ],
     "width": 130
    }
   ]
  }
 ]
}

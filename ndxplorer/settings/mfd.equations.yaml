- Sg: >-
    'Green Count Rate (KHz)'
- Sr: >-
    'Red Count Rate (KHz)'
- Sg/Sr: >-
    'Sg' / 'Sr'
- Proximity ratio: >-
    'Sr' / ('Sg' + 'Sr')
- Fg: >-
    'Sg' - 'Bg'
- Fr: >-
    'Sr' - 'Br' - 'alpha' * 'Sg'
- Fg/Fr: >-
    'Fg' / 'Fr'
- Fd/Fa: >-
    'Fg/Fr' / 'gG/gR'
- FRET efficiency: >-
    (1.+ 'Fd/Fa' * 'PhiA' / 'PhiD')**(-1.0)
- R_FRET: >-
    'forster_radius' * (1. / 'FRET efficiency' - 1.)**(1./6.)
- Sg(PIE): >-
    'S prompt green (kHz)'
- Fg(PIE): >-
    'Sg(PIE)' - 'Bg'
- Sr(PIE): >-
    'S prompt red (kHz)'
# 'beta' is ndX's name for the direct-excitation coefficient (Hellenkamp's
# delta): the fraction of the acceptor-excitation signal that the DONOR laser
# also produces. It was defined, carried and pushed in by the calibration --
# and never used here, so every efficiency ignored direct excitation however
# carefully it had been determined. Restoring the term makes this identical to
# chisurf.core.fluorescence.burst.es.corrected_es.
- Fr(PIE): >-
    'Sr(PIE)' - 'Br' - 'alpha' * 'Sg(PIE)' - 'beta' * 'Fy(PIE)'
- Sy(PIE): >-
    'S delayed yellow (kHz)'
- Fy(PIE): >-
    'Sy(PIE)' - 'By'
- Fg/Fr(PIE): >-
    'Fg(PIE)' / 'Fr(PIE)'
- Sg/Sr(PIE): >-
    'Sg(PIE)' / 'Sr(PIE)'
- Fd/Fa(PIE): >-
    'Fg/Fr(PIE)' / 'gG/gR'
- Proximity ratio(PIE): >-
    'Sr(PIE)' / ('Sr(PIE)' + 'Sg(PIE)')
- FRET efficiency(PIE): >-
    (1.+ 'Fd/Fa(PIE)' * 'PhiA' / 'PhiD')**(-1.0)
- R_FRET(PIE): >-
    'forster_radius' * (1. / 'FRET efficiency' - 1.)**(1./6.)
- ratioA(PIE): >-
    'Fr(PIE)'/ 'Fy(PIE)'
- FRET efficiency_AA(PIE): >-
    'ratioA(PIE)' / 'r'
- R_FRET,AA(PIE): >-
    'forster_radius' * (1. / 'FRET efficiency_AA(PIE)' - 1.)**(1./6.)
- Sapp(PIE,S): >-
    ('Sr(PIE)' + 'Sg(PIE)') / ('Sr(PIE)' + 'Sy(PIE)' + 'Sg(PIE)')
- Sapp(PIE,F): >-
    ('Fr(PIE)' + 'Fg(PIE)') / ('Fr(PIE)' + 'Fy(PIE)' + 'Fg(PIE)')
# Fr(PIE) already has the leakage and the direct excitation removed, so the
# numerator must not remove them again -- it used to subtract both a second
# time here, which is why S disagreed with the accurate value even when every
# constant was right.
- Stoichiometry (PIE): >-
    ('Fr(PIE)' + (1/'gG/gR') * ('PhiA'/'PhiD') * 'Fg(PIE)') / ('Fr(PIE)' + (1/'gG/gR') * ('PhiA'/'PhiD') * 'Fg(PIE)' + 'r' * 'Fy(PIE)')
- Tg-Tr(ms): >-
    'Duration (green) (ms)' - 'Duration (red) (ms)'
- <tauD(A)>x: >-
    (1 - 'FRET efficiency') * 'tauD0'
- Var_tauD(A)): >-
    'Tau (green)' * '<tauD(A)>x' - '<tauD(A)>x'**(2.0)
- Total count rate green + red: >-
    'Green Count Rate (KHz)' + 'Red Count Rate (KHz)'
- 1/Sensitivity: >-
    1./(1. - 'FRET efficiency' * (1. - ('gG/gR' * 'PhiD' / 'PhiA')))

# ---- ALEX-identified rates from counts (+ duration) ----
- Sg(ALEX): >-
    'F_Dexc_Dem' / 'Tau' / 1.0e3
- Sr(ALEX): >-
    'F_Dexc_Aem' / 'Tau' / 1.0e3
- Sy(ALEX): >-
    'F_Aexc_Aem' / 'Tau' / 1.0e3  # acceptor-excitation acceptor-emission
- Sx(ALEX): >-
    'F_Aexc_Dem' / 'Tau' / 1.0e3  # acceptor-excitation donor-emission (usually leakage)

# ---- Background / leakage corrected ALEX channels ----
# expects same constants as PIE: Bg, Br, By, alpha, beta, r, gG/gR, PhiA, PhiD
- Fg(ALEX): >-
    'Sg(ALEX)' - 'Bg'
# The direct-excitation term, as in Fr(PIE) above -- it was missing here too.
- Fr(ALEX): >-
    'Sr(ALEX)' - 'Br' - 'alpha' * 'Sg(ALEX)' - 'beta' * 'Fy(ALEX)'
- Fy(ALEX): >-
    'Sy(ALEX)' - 'By'

# ---- Derived ALEX ratios and efficiencies ----
- Fg/Fr(ALEX): >-
    'Fg(ALEX)' / 'Fr(ALEX)'
- Sg/Sr(ALEX): >-
    'Sg(ALEX)' / 'Sr(ALEX)'
- Fd/Fa(ALEX): >-
    'Fg/Fr(ALEX)' / 'gG/gR'
- Proximity ratio(ALEX): >-
    'Sr(ALEX)' / ('Sr(ALEX)' + 'Sg(ALEX)')
- FRET efficiency(ALEX): >-
    (1. + 'Fd/Fa(ALEX)' * 'PhiA' / 'PhiD')**(-1.0)

# ---- Apparent stoichiometry from signals and from corrected fluxes (ALEX) ----
- Sapp(ALEX,S): >-
    ('Sr(ALEX)' + 'Sg(ALEX)') / ('Sr(ALEX)' + 'Sy(ALEX)' + 'Sg(ALEX)')
- Sapp(ALEX,F): >-
    ('Fr(ALEX)' + 'Fg(ALEX)') / ('Fr(ALEX)' + 'Fy(ALEX)' + 'Fg(ALEX)')

# ---- Full ALEX stoichiometry (same form as PIE, just using ALEX channels) ----
# Fr(ALEX) already has leakage and direct excitation removed; not again here.
- Stoichiometry (ALEX): >-
    ('Fr(ALEX)' + (1/'gG/gR') * ('PhiA'/'PhiD') * 'Fg(ALEX)') /
    ('Fr(ALEX)' + (1/'gG/gR') * ('PhiA'/'PhiD') * 'Fg(ALEX)' + 'r' * 'Fy(ALEX)')

# ---- Convenience sums (ALEX) ----
- Total count rate (ALEX): >-
    'Sg(ALEX)' + 'Sr(ALEX)'

# ---- FRET factors ----
- Rapp: >-
    (3./2.) / ('kappa2') * 'RDA'
- kFRET: >-
    1/'tauD0'*3/2*'kappa2'*('forster_radius'/'RDA')**(6.0)

# ---- Diffusion from td4 diffusion times (weak-matched columns) ----
# Uses D = w_r^2 / (4 * tau_D); here w_r in µm, tau_D in ms, so
# D(µm²/s) = 250 * w_r(µm)^2 / tau_D(ms).
#
# The base td4 columns are written as 'td_mean' / 'td_peak' (and
# per-pair variants like 'td_mean__GG'). The alias 'td_' below always
# refers to the mean diffusion time, with weak matching taking care of
# optional suffixes.
- td_: >-
    'td_mean'
- D_um2_s_from_td: >-
    250.0 * 'omega_r_um'**2 / 'td_'

# Hydrodynamic radius from D using Stokes–Einstein:
#   r_h = k_B T / (6 π η D)
# with D in µm²/s and the result expressed in nm.
- Rh_nm_from_D: >-
    ('KB' * 'T_K' * 1.0e21) / (18.84955592153876 * 'eta_Pa_s' * 'D_um2_s_from_td')

# =====================================================================
# FRET-line transformations of E and <tauD(A)>F  (Barth et al., J. Chem.
# Phys. 156, 141501 (2022) — "Theory of FRET-lines").
#
# These build the alternative representations of the two FRET observables
# used in the variance plot (Fig. 3) and the moment plot (1-E)*E_tau vs E
# (Fig. 8 / Eq. 44–45). They reuse the already-defined columns:
#   'FRET efficiency'  = E              (intensity-based FRET efficiency)
#   'Tau (green)'      = <tauD(A)>F     (fluorescence-weighted donor lifetime)
#   '<tauD(A)>x'       = (1-E)*tauD0    (species-weighted lifetime, Eq. 22)
#   'Var_tauD(A))'     = Var(tauD(A))   (= <tauD(A)>F*<tauD(A)>x - <tauD(A)>x^2, Eq. 34)
# and the constant 'tauD0' = tauD(0).
# (Columns referencing a missing input are silently skipped, so non-lifetime
#  data sets are unaffected.)

# Intensity-weighted FRET efficiency from the fluorescence-weighted lifetime
#   E_tau = 1 - <tauD(A)>F / tauD(0)            (Eq. 35)
- E_tau: >-
    1. - 'Tau (green)' / 'tauD0'

# Variance of the FRET-efficiency distribution from the lifetime moments
#   Var(E) = Var(tauD(A)) / tauD(0)^2           (Eq. 36)
#          = (1-E) * (E - E_tau)                (equivalent closed form, Eq. 34)
# This is the lifetime-based analogue of the BVA variance (Fig. 3).
- Var(E): >-
    'Var_tauD(A))' / 'tauD0'**2.0

# Standard deviation of E (BVA-comparable y-axis of Fig. 3, top panels).
# Magnitude of Var(E); bursts below the static line (Var(E)<0 from noise)
# are mapped to their absolute value before the square root.
- sigma_E: >-
    abs('Var(E)')**0.5

# Moment-difference representation Gamma = (1-E) * E_tau   (Eq. 44; y-axis of Fig. 8)
#   Gamma = (1-E) * (1 - <tauD(A)>F/tauD0) = (1-E) * E_tau
- (1-E)*E_tau: >-
    (1. - 'FRET efficiency') * 'E_tau'

# Static reference line in the moment representation:
#   Gamma_static = (1-E) * E                    (Eq. 45)
# A burst with no sub-burst dynamics (E_tau == E) falls on this parabola.
- (1-E)*E: >-
    (1. - 'FRET efficiency') * 'FRET efficiency'
